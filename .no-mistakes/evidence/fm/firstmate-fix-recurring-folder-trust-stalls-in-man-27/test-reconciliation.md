# Pi trust test reconciliation
No additional live Herdr/Pi launch was performed in this turn.
Retained pi-trust-final.log from this run records the trust dialog, managed scout --approve launch, model-created marker, unchanged trust stores, same child worktree parent-trust comparison, and missing-capability refusal (controlled help response, real fm-spawn).
Broader worker/scout/secondmate/relaunch live guard coverage is registered from the explicit accepted user reconciliation; the retained log independently corroborates scout and refusal only.
No distinct pi-signed executable is on PATH. The accepted reconciliation says installed pi execs the signed launcher; its private report is absent here. Shared Pi-family portable regression covers the launch template, not a distinct live wrapper.

Current checks:
- TMPDIR=/tmp bash tests/fm-spawn-dispatch-profile.test.sh: exit 0.
- TMPDIR=/tmp bash tests/fm-secondmate-restart.test.sh: exit 0.
- TMPDIR=/tmp bash tests/fm-control-relaunch.test.sh: Pi approval check passed; later backlog reverification exited 1. Installed tasks-axi --version is 0.2.5; product requires 0.2.6 or newer with update/mv capabilities.
- Initial runs with TMPDIR=$PWD/.test-tmp hit the prohibition on secondmate homes inside the repository; reruns used permitted incidental toolchain temporary files.
No screenshots: retained evidence is terminal capture from an already completed live lab; no live launch was recreated for imagery.
